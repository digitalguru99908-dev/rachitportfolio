services:
  - type: web
    name: rachit-portfolio-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: RESEND_API_KEY
        sync: false
      - key: CONTACT_EMAIL
        sync: false