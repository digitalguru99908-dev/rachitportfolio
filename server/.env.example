# server/ folder — backend for the portfolio site. Deployed on Render.
# Copy this file to server/.env ONLY for local testing (gitignored).
# On Render these are set via dashboard env vars instead.

RESEND_API_KEY=
CONTACT_EMAIL=